import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    comentarios: {
        flexDirection: "row",
        alignItems: "center",
    },

    img: {
        width: 40,
        height: 40,
        marginRight: 5,
    }
})

export default styles;